import AddPageUIUtil from './page_ui.js';


const dnPage = function(pageObj){

 
 // 添加一些通用组件
 AddPageUIUtil(pageObj);
  /**
   * 添加一系列生命周期处理函数
   */
  Page(pageObj)

  return pageObj
}

export default dnPage